[project]
name = "langgraph-book"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "langchain-openai>=1.6.0",
    "langgraph>=1.2.11",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
